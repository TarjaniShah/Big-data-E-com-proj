1. Download elastic search and run the elasticsearch.bat
2. Download the logstash plugin files and run the logstash.conf file in the bin folder to tranfer data from mysql to ES(scheduling can be done in this config file)
3. Open the 'riipen_master' project using Netbeans IDE
4. Start the server by running the npm start command from the riipen_master folder.